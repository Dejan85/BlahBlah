# ✅ BlahBlah — Task lista (redosled rada)

> Radni tracker — radimo **task po task, ovim redom**. Čekiraj `[x]` kad je gotovo.
> Pravila: svaka mehanika ide kroz `lib/` + test (`ARCHITECTURE.md` §2.5). Posle svakog taska: `tsc` prolazi.
> Reference: `FEATURES.md` (mehanike), `SCREENS.md` (ekrani), `PROJECT_STATUS.md` (status).

**Ukupno: ~54 task** · Faza 0: 5 · Faza 1: 9 · Faza 2: 3 · Faza 3: 24 · Faza 4: 13

**Legenda:** 🔧 tehnički (ja sam) · 🧠 traži tvoju odluku · 🧪 ima `lib/` test · ☁️ backend/DB job · ⚡ **preporučen High effort** (zamršena logika/ivični slučajevi — bumpni effort pre ovog taska)

> ⚡ Kad dođemo do taska sa ⚡, podsetiću te da digneš effort na High; marker je ovde da se NE oslanjamo na pamćenje.

---

## FAZA 0 — Da app proradi (🔧, bez odluka) — *cilj: `expo start` se diže* — ✅ ZAVRŠENA
- [x] **T0.1** ✅ Izabran **npm** (obrisan `yarn.lock`, zadržan `package-lock.json`, `npm install` prošao)
- [x] **T0.2** ✅ **Uklonjen `firebase` JS SDK** (mrtav kod): skinut iz `package.json`, obrisan `utils/firebase.ts`, uklonjen import iz `_layout.tsx` + re-export iz `utils/index.ts`. firebase nestao iz node_modules, TS greške 18→17.
- [x] **T0.3** ✅ Popravljen case-mismatch u `app.json` (`GoogleService-info.plist` → `GoogleService-Info.plist`); poklapa se sa fajlom na disku
- [x] **T0.4** ✅ ~~Popraviti firebase `appId`~~ — **otpalo** (uklonjeno u T0.2)
- [x] **T0.5** ✅ App pokrenut na realnom uređaju (Galaxy S24) — diže se bez crash-a do login ekrana. Usput: `expo install --fix` poravnao 7 paketa na SDK-51 verzije (rešilo native gesture-handler crash); Supabase bila pauzirana → reaktivirana.

## FAZA 1 — Čist kod / temelj (🔧) — ✅ ZAVRŠENA
- [x] **T1.1** ✅ Popravljeno svih 17 TS grešaka → `tsc --noEmit` prolazi čisto. Join greške (notifications/followers/following): supabase to-one relacija tipovana kao niz → normalizacija na objekat / `as unknown` cast. `currentLocation` tipovan `Location.LocationObject | null`. `components/Acounts.tsx` (mrtav Supabase starter, nigde se ne importuje) obrisan.
- [x] **T1.2** ✅ Uklonjeni `console.log` koji su ispisivali Supabase URL + anon key (prvih 50 char + dužina) u `utils/supabase.ts`. Zamenjeno tihim `console.warn` guard-om koji javlja samo da env nedostaje (bez vrednosti). Provereno: nigde drugde se ključevi ne loguju.
- [x] **T1.3** ✅ Uklonjen mrtav `handleMessageReaction` iz `MessageContext` (nikad eksportovan; pravi je `handleReaction`). `tsc` čist. ⚠️ `profile/test/[id].tsx` **NIJE** mrtav kod — koristi se (chats/index.tsx, PostUserInfo.tsx) i nije duplikat: to je nedovršen prototip ujedinjenog profila → izdvojeno u **T1.8 + T1.9**.
- [x] **T1.4** ✅ Preimenovan folder `freind-requests` → `friend-requests` (`git mv`, čuva istoriju) + ažurirane rute (`chats/index.tsx`, `Push.tsx`). `tsc` čist.
- [x] **T1.5** ✅ `package.json` name `test` → `blahblah`. README zamenjen sa default Expo starter-a → pravi (tech stack, env varovi `EXPO_PUBLIC_SUPABASE_*`, komande, struktura, konvencije, link na `docs/`). `tsc` čist.
- [x] **T1.6** ✅ Jest infra potvrđena: `@/*` alias mapiran u jest config (`moduleNameMapper`), `test` skripta → jednokratni `jest` (+ `test:watch`), smoke test `lib/smoke.test.ts` (TS+Jest+alias) prolazi 2/2. `tsc` čist.
- [x] **T1.7** ✅ ESLint (`eslint-config-expo`) + Prettier postavljeni, prolaze čisto (0 errors). Popravljena 2 prava errora (rules-of-hooks `useImage`/FilterMenu, `__dirname`/metro.config). Očišćeno ~145 mrtvih importa/varijabli (`eslint-plugin-unused-imports` + ručno) + 5 display-name. `.prettierignore` preskače `docs/`/native/`*.md`. **Ostaje 40 `exhaustive-deps` warning — namerno** (popravka menja ponašanje). `tsc` čist, test 2/2.
- [x] **T1.8** ✅ **ODLUKA: 2 ekrana po spec-u** (Figma 7.x tuđi vs 8.x svoj). Base fajlovi: **svoj = `profile/index.tsx`** (grid radi, edit, realtime), **tuđi = `profile/profile-details/[id].tsx`** (grid radi, follow/block/message, private-lock, navigacija na dedicated followers/following liste). **Briše se `profile/test/[id].tsx`** (nedovršen unify prototip: grid zakomentarisan l.638/648, duplirani render blokovi). Ključ: app već de facto radi kao 2 ekrana — skoro sve rute idu na `profile-details/[id]`, samo 2 call-site-a gađaju `test/[id]`. Feature-matrica i obrazloženje: vidi §"Odluke" niže. *Odblokira T1.9.*
- [x] **T1.9** ✅ **Konsolidacija profila — 2 ekrana** (odluka T1.8). Obrisan `profile/test/[id].tsx` (`git rm`, čuva istoriju). Rewire 2 call-site-a + očišćene 2 zakomentarisane reference. **Korekcija na doslovni task:** `chats/index.tsx:340` `navigateToProfile` je **svoj** nalog (header avatar = ulogovani `currentUserId`) → ide na **`/profile`** (svoj, index.tsx), NE `profile-details` (inače Follow/Block na sebi). `PostUserInfo.tsx:97` grana: svoj post (`userId === currentUser.id`) → `/profile`, tuđi → `profile-details/[id]` (profile-details renderuje Follow/Message bezuslovno, pa svoj mora da preskoči). Mrtvi blokovi obrisani: `PostUserInfo` l.64–94 (stari `isOwnProfile` switch), `GridPost` l.385–392 (`handleProfilePress`). **Deljeni avatar/stats blok NIJE izvučen** (bilo "po potrebi") — ekrani namerno različiti (T1.8 matrica), oba rade i pokrivaju spec 7.x/8.x; ekstrakcija = rizik bez koristi. `tsc` čist, ESLint 0 errors, test 2/2. *Odblokira T3.4, T3.20, T3.21.*

## FAZA 2 — Backup baze (🔧 ☁️) — ✅ ZAVRŠENA
- [x] **T2.1** ✅ Šema u repo. `supabase init` + `link` (ref `whgjngkbhwjnwuupjkxn`, region `eu-central-2`). `db pull`/`db dump` traže Docker (shadow DB) → **nije instaliran** → umesto toga **lokalni `pg_dump 17` direktno na pooler** (instaliran `PostgreSQL.PostgreSQL` preko winget; pg_dump 17 nad PG15 serverom radi). Snapshot: `supabase/migrations/20260624145146_remote_schema.sql` (50KB, schema-only public): **17 tabela, 16 funkcija, 53 RLS politike, 17 ENABLE RLS, 7 trigera**. Root `.gitignore` skinuo blanket `supabase/` (migracije sad u repo), tajne (PAT+DB pass) u gitignorovanom `supabase/.env.local`. ⚠️ Snapshot je za verzionisanje, **ne replay** (pg_dump17 ubacuje `CREATE SCHEMA public` + `SET transaction_timeout` koji PG15 ne zna). *Usput pokriva i većinu T2.3 (RLS politike unutra).*
- [x] **T2.2** ✅ TS tipovi generisani `supabase gen types typescript --linked` → `types/database.types.ts` (895 l, 17 tabela). Izloženi kroz `@/types`: `Database`, `Json` + helper-i `Tables<'x'>`/`TablesInsert`/`TablesUpdate`. Dodata `npm run gen:types` skripta. **Database generic NIJE prosleđen globalno u `createClient`** (svesno): wiring surfacuje 51 neusklađenost — cast cleanup po fajlovima (drugi taskovi) + **otkriće: kod gađa tabele `friends`/`friend_requests` koje NE postoje u šemi** (žive su `follows`/`follow_requests`) → latentni bug, prijavljen u PROJECT_STATUS §4. Tipovi sad dostupni za eksplicitnu upotrebu (Faza 3 `lib/`). `tsc` čist, ESLint 0, test 2/2.
- [x] **T2.3** ✅ Storage bucket-i + storage RLS izvučeni iz žive baze (`psql` nad `storage.buckets` + `pg_policies` schema=storage) i verzionirani u `supabase/storage_buckets_and_policies.sql`: **4 bucket-a** (`avatars` **private**; `posts`/`audio-messages`/`chat-files` javni) + **10 storage RLS politika** na `storage.objects`. Public-šema RLS (53) već u `..._remote_schema.sql` (T2.1). ARCHITECTURE §4 lista bucket-a ispravljena (fali­o `chat-files`). 🔴 **Nalaz:** `avatars` je private a kod koristi `getPublicUrl('avatars')` → public URL ne radi na privatnom bucket-u (prijavljeno u PROJECT_STATUS §4).

## FAZA 3 — Signature mehanike (🧪 `lib/` + testovi) — *najveći blok*
> Redosled: Blah Score je temelj (od njega zavise streak, recovery, score boost).

**Blah Score**
- [x] **T3.1** ✅ `lib/formatCount.ts` — kompaktan prikaz brojeva (1000→"1k", 10000→"10k", 10100→"10.1k", 1.2M, 2.5B). Čista funkcija (§2.5): `Number.isFinite` guard (→"0"), `<1000` ceo broj (256.8→"257"), tier-i k/M/B po veličini, mantisa `toFixed(1)` sa skidanjem ".0", rollover preliva u sledeću jedinicu (999_999→"1M", ali 999_500→"999.5k"), negativni znak. Test `lib/formatCount.test.ts` (10 grupa, sve prolaze). `tsc` čist, ESLint 0. ⚠️ Postoji duplikat inline `formatNumber` u `PostActions.tsx:68` (veliko "K") — zamena lib funkcijom u T3.4 (integracija u UI).
- [x] **T3.2** ✅ `lib/blahScore.ts` — `calculateBlahScore(blahsSent, followers, streakDay)`: `(Blahs×4)+(Followers×0.8)+Streak Bonus`, bonus `=Blahs×2` SAMO na danima 8/20/28/48, `Math.round` na kraju, `Number.isFinite` guard (NaN/∞→0, skor nikad NaN). Test `lib/blahScore.test.ts` (9 grupa, prolaze). ⚠️ **Doc-fix:** ARCHITECTURE §2.5 primer je imao tipfeler `(10, 50, 8)` koji daje **100**, ne 68 → ispravljeno na `(10, 10, 8)` = 40+8+20 = 68 (poklapa se sa "8. dan = 68"). `tsc` čist, ESLint 0.
- [x] **T3.3** ✅ DB: `blah_score` storage kolone + migracija. Dodato na `public.profiles` (migracija `supabase/migrations/20260624162117_blah_score_columns.sql`, primenjena live preko psql na pooler): **`blah_score`** (keširan/prikazani skor) + **`blahs_sent`** (denormalizovan brojač, ulaz u formulu) — obe `integer NOT NULL DEFAULT 0` (aditivno, rollback = DROP COLUMN). **Svesno uže od „kolone/tabela":** bez `followers` kolone (→ `COUNT` iz `follows`, bez drift-a), bez `streak_day` (→ T3.6), **bez SQL funkcije za skor** (formula ostaje u `lib/blahScore.ts` — pravilo „logika u lib/"; DB samo skladišti, T3.4 računa+upisuje). TS tipovi regenerisani (`gen:types` → `database.types.ts` Row/Insert/Update). `tsc` čist.
- [x] **T3.4** ✅ Integracija u UI. `app/profile/index.tsx` (svoj profil): fetch `blah_score`/`blahs_sent`, obračun preko `calculateBlahScore(blahs_sent, followersCount, 0)` (`streakDay=0` dok streak nije implementiran — T3.6), keširan skor se upisuje u `profiles.blah_score` samo kad se promenio (bez suvišnih write-ova). Prikaz `formatCount(blahScore)` umesto hardkodovanog "10.7k"; **Blahs stat u crvenom** (`#FF325E`) kad je skor > 0 (MyProfile 8.9 — aktivan Blah Score). Usput: zamenjen duplikat inline `formatNumber` (veliko "K") u `components/PostActions.tsx` sa `lib/formatCount` (jedinstven prikaz, malo "k"). `tsc` čist, ESLint 0, test 21/21.

**Streak + Recovery**
- [x] **T3.5** ✅ `lib/streak.ts` — streak obračun + reset + bonus dani. **Model (spec meša 2 tajmera → razdvojeni po nameni):** (1) **brojanje/reset po KALENDARSKOM danu** (timezone-aware preko `tzOffsetMinutes`): `registerBlah(prev, now, tz)` — isti dan = bez promene (pamti najkasniji Blah), uzastopni dan = +1, preskočen ceo dan = reset na 1, skew unazad = bez promene; `getStreakStatus` → `none|active|at-risk|lost`; `currentStreakDay` → 0 kad `lost` (vrednost za `calculateBlahScore`). Kalendar (ne čist rolling 24h) jer "prvi Blah u 24h se računa" (A2) traži fiksnu dnevnu mrežu — odatle i ivični slučajevi "granice dana/timezone". (2) **Bunny rolling deadline** (Home 2.0): `msUntilDeadline`/`isBunnyActive` — ≤3h pre 24h od poslednjeg poslatog Blah-a. (3) 26h recovery → ostaje T3.7. **Anti-drift:** `STREAK_BONUS_DAYS` (8/20/28/48) izvučen u `streak.ts` kao single source; `blahScore.ts` sad uvozi `isStreakBonusDay` (uklonjen lokalni `STREAK_DAYS` duplikat). Test `lib/streak.test.ts` (6 grupa: bonus dani, registerBlah uklj. timezone granice, status, currentStreakDay, rolling/bunny) — `tsc` čist, ESLint 0, **test 49/49** (4 suite). ⚠️ Profil još šalje `streakDay=0` (T3.4) — uvezivanje pravog dana u UI dolazi sa streak DB (T3.6).
- [x] **T3.6** ✅ Streak DB + background reset job. **Kolone** na `profiles` (migracija `20260624170500_streak_columns_and_reset_job.sql`, primenjena live): `streak_day` / `last_blah_at` (timestamptz) / `streak_tz_offset` (minuti, CET=+60). **Reset job = pg_cron + tanak SQL sweep** (`reset_lapsed_streaks()`, zakazan `'0 * * * *'`): nulira streak kad je razmak ≥2 kalendarska dana (tz-aware), atomski single-UPDATE → concurrency-safe. **Odluka (vidi §Odluke):** math OSTAJE u `lib/streak.ts`; SQL sweep je VERAN PORT JEDNE granice (`getStreakStatus`→`'lost'`), **zakovan PINNING testom** u `streak.test.ts` (SQL formula == `isStreakLost` oko granice, tz 0/+60/−300/+330). Advance (+1/reset-na-slanje) se NE portuje u SQL — radi ga klijent (`registerBlah`, anti-drift kao T3.3). **Wiring:** (1) `app/blahs/new.tsx` slanje → `registerBlah` + upis `streak_day`/`last_blah_at`/`streak_tz_offset` **+ inkrement `blahs_sent`** (usput: bio nikad inkrementiran → skor nije rastao; A2 real-time increment deo zatvoren); (2) `app/profile/index.tsx` → `currentStreakDay(state, now, tz)` umesto `streakDay=0` (pravi bonus 8/20/28/48) + **on-read lazy reset** (backup cronu). Novi `lib/streak.ts` `isStreakLost`. `pg_cron` enable-ovan live preko pooler-a (radilo). `tsc` čist, ESLint 0, **test 53/53**.
- [x] **T3.7** ✅ `lib/blahRecovery.ts` — vremenski prozori recovery-ja (⚡ High effort). **Model ROLLING (anchor = poslednji Blah), namerno odvojen od kalendarskog brojanja u `streak.ts`:** osa `0 → 26h (safe) → 39h (recoverable, ponuda 13h) → expired`. API: `streakLostAt`/`recoveryOfferExpiresAt` (epoch granice), `getRecoveryStatus` → `safe|recoverable|expired` (tačno na 26h → `recoverable`, tačno na 39h → `expired`), `isRecoveryAvailable`, `msUntilStreakLost` (26h countdown), `msUntilOfferExpires` ("In 13h offer expire" countdown), `isRecoveryUrgent` (urgency bunny u poslednja 3h pre pada — razlikuje se od `isBunnyActive` koji je 24h). Konstante `RECOVERY_GRACE_MS`/`OFFER_MS`/`URGENCY_MS`/`PRICE_EUR=1.99`. Tz-nezavisno (sati apsolutni → bez `tzOffset`). `null` lastBlahAt / nevažeći `now` → bezbedan default (`expired`/`null`/`0`). Test `lib/blahRecovery.test.ts` (8 grupa, granice 26h/39h/3h + null/NaN). `tsc` čist, ESLint 0, **test 72/72** (5 suite, +19). Plaćanje (RevenueCat) i popup → T3.8.
- [x] **T3.8** ✅ Recovery popup (MyProfile 8.8) + plaćanje. **Odluka (vidi §Odluke): stubbed purchase boundary** — RevenueCat ključevi/store proizvodi još ne postoje, pa je IO sloj `services/recoveryPurchase.ts` (`purchaseRecovery()` + `RECOVERY_PURCHASE_STUBBED` prekidač) STUB koji simulira uspeh; pravi `Purchases.purchasePackage` tok je dokumentovan u komentaru, potpis ostaje isti → flip prekidača kad budu proizvodi. **`lib/blahRecovery.ts` +2 čiste funkcije:** `applyRecovery(restoredDay, now)` (state posle plaćanja: streak se vraća na sačuvani dan + nov 26h anchor) i `formatRecoveryCountdown(ms)` ("In 13h offer expire"). **Wiring `app/profile/index.tsx`:** `getRecoveryStatus` iz `last_blah_at`; kad `recoverable` → zapamti dan + auto-otvori popup (jednom/mount, `useRef` guard) sa live 13h countdown-om (interval 60s); na Continue → `purchaseRecovery` → `applyRecovery` → upis `streak_day`/`last_blah_at` → refetch skora. **Anti-drift fix:** on-read lazy reset (T3.6) sad gejtovan — nulira `streak_day` tek kad recovery `expired`, NE dok je `recoverable` (čuva dan za vraćanje). **`PremiumModal`** dobio `offerSubtitle` (dinamičan countdown umesto hardkodovanog) + `processing` (spinner na Continue). `tsc` čist, ESLint 0, **test 79/79** (+7). ⚠️ Watch: pg_cron sweep je i dalje kalendarski (gap≥2) → uzak prozor gde cron može nulirati `streak_day` serverski pre nego što korisnik recover-uje; in-memory `restoredDay` pokriva tekuću sesiju. Urgency bunny animacija (`isRecoveryUrgent`) ostaje za streak UI badge polish.
- [x] **T3.9** ✅ "Blah Streak Lost" notifikacija (D6). **Otkriće:** UI render za D6 **već postoji** u `components/Push.tsx` (tip `'BLAHS'` → "Oops! You lost your blahs"), ali se notif. nigde nije kreirala niti deep-link-ovala. Dodato: (1) **čista odluka u `lib/blahRecovery.ts` `shouldNotifyStreakLost({recoveryStatus, streakDay, lostAt, lastNotifiedAt})`** — javlja samo kad je `recoverable` + `streakDay>0` + nije već javljeno; **dedup preko `lastNotifiedAt >= lostAt`** (svaki nov pad ima kasniji `streakLostAt` → novi anchor) + test (7 grupa). (2) **Wiring `app/profile/index.tsx`** — u postojećoj `recoverable && day>0` grani poziva `maybeCreateStreakLostNotification` (modul-helper, I/O): upit `created_at` zadnje `'BLAHS'` notif. → odluka → insert (`sender_id=recipient_id=userId`, sistemska notif. bez pravog pošiljaoca; `payload {kind:'streak_lost', lostDay}`). On-read kreiranje (kao ostatak streak/recovery wiringa; pravi push = T4.3). (3) **Deep-link:** `Push.tsx` `handlePress` tip `'BLAHS'` → `/profile` (recovery popup se tamo auto-otvara dok je `recoverable`); `notifications/index.tsx` tip-cast proširen sa `'FOLLOW_REQUEST'|'MESSAGE'` na `Notification['type']` (uključuje `'BLAHS'`). `tsc` čist, ESLint 0, **test 86/86** (+7). ⚠️ Watch: ako korisnik prvi put otvori profil tek posle 39h (`expired`), notif. se ne kreira (on-read prozor ograničen na `recoverable`) — potpuna pokrivenost traži background push (T4.3).

**Chat Hours**
- [x] **T3.10** ✅ `lib/chatHours.ts` — Chat Hours brojač (⚡ High effort). **Model (Snapchat „snapstreak", brojano u SATIMA):** serija je ŽIVA samo dok su OBA učesnika poslala bar jednu poruku u poslednja 24h → rok isteka `chatExpiresAt = min(lastFromMe, lastFromThem) + 24h` (vezuje strana koja je poslala najdavnije; spam jedne strane NE drži streak — to je ⚡ ivični slučaj). Dok je živa, `chatHours = floor((now − startedAt)/1h)` (npr. „83h"/„4783h", SCREENS.md Chat 5.0/24); kad istekne → 0; nova obostrana aktivnost u 24h prozoru pokreće NOVU seriju od ranijeg sidra. API: `registerMessage(state, 'me'|'them', now)` (reset po poruci, skew-safe), `getChatHoursStatus` → `none|active|at-risk|expired` (at-risk = poslednja 3h pre isteka, peščani sat), `chatHours`, `chatExpiresAt`, `msUntilChatExpiry`, `formatChatHours(n)` → „{n}h". Konstante `CHAT_HOURS_WINDOW_MS` (24h) / `CHAT_HOURS_WARNING_MS` (3h). Čista funkcija (§2.5), `Number.isFinite` guardovi. Test `lib/chatHours.test.ts` (10 grupa: obostranost, reset/produženje, istek→nova serija, skew, granice 24h/3h, veliki brojač 4600h). `tsc` čist, ESLint 0, **test 111/111** (+25). *Prikaz u listi chatova → T3.11.*
- [x] **T3.11** ✅ Chat Hours prikaz u listi chatova (sati „h" — Chat 5.0). **Bez nove lib logike** — wiring T3.10 (`lib/chatHours`) u UI. `app/chats/index.tsx`: `fetchConversations` rekonstruiše `ChatHoursState` po konverzaciji **fold-om `registerMessage` preko SVIH poruka hronološki** (pošiljalac `'me'` ako je trenutni korisnik, inače `'them'` → obostranost drži streak); real-time INSERT subscription sad foldira i **moju** poruku (ne samo primljenu) da serija raste. Mapiranje za listu derivira `chatHours = computeChatHours(state, now)` + `chatHoursAtRisk` (`getChatHoursStatus==='at-risk'`). `components/Chat/ChatListItem.tsx`: desno od username/preview-a `formatChatHours(item.chatHours)` (npr. „83h") kad `>0`; boja brand `#FF325E`, **at-risk** (≤3h pre isteka) → narandžasta `#FF9500`. Tipovi: `EnhancedConversation.chatHoursState`, `User.chatHours`/`chatHoursAtRisk`. `tsc` čist, ESLint 0, test 111/111 (logika nepromenjena). ⚠️ Brojač se ne „kuca" uživo (sati sporo rastu — osvežava se na fetch/novu poruku); ephemeral retencija poruka (24h) → T3.12+.

**Ephemeral chat**
- [x] **T3.12** ✅ `lib/ephemeral.ts` — retencija poruka (⚡ High effort). **Model:** retencioni prozor je svojstvo konverzacije (toggle `saved`): `false`→24h (`EPHEMERAL_DEFAULT_MS`), `true`→30d (`EPHEMERAL_SAVED_MS`). Rok poruke = `createdAt + retentionMs(saved)`; istekla kad `now >= expiresAt` (granica = isteklo, dosledno `chatHours`). API: `retentionMs`, `messageExpiresAt`, `isMessageExpired` (UI/placeholder), `msUntilMessageExpiry`, `expiryCutoff` (batch prag za T3.13 SQL sweep: `createdAt <= cutoff`). **⚡ Ivični slučaj:** toggle se primenjuje na trenutno stanje (rok se rekalkuliše iz aktuelnog `saved`) — uključ. „Save chat" produžava neobrisane poruke na 30d, isključ. skraćuje na 24h pa poruke >24h postaju odmah expired; poruka koju je job VEĆ obrisao se ne vraća (concurrency = T3.13). **Anti-drift:** ekvivalencija per-poruka (`isMessageExpired`) == batch (`expiryCutoff`) zakovana pinning testom oko granica 24h/30d. Fail-safe guardovi: nevažeći `createdAt`/`now` → ne briši (`false`/`null`/`0`). Test `lib/ephemeral.test.ts` (10 grupa: konstante, granice 24h/30d, toggle menja sudbinu poruke, clock skew, cutoff, pinning). `tsc` čist, ESLint 0, **test 138/138** (7 suite, +27). *DB + auto-brisanje job → T3.13; „Save chat" toggle UI + „Deleted message…" placeholder → T3.14.*
- [x] **T3.13** ✅ Ephemeral DB + auto-brisanje poruka job (⚡ High effort). **Kolona** `conversations.saved` (boolean NOT NULL DEFAULT false) — retencioni toggle (24h `false` / 30d `true`; UI = T3.14). **Job = pg_cron + SQL sweep** (`delete_expired_messages()`, zakazan `'5 * * * *'` — :05 da se ne sudara sa T3.6 streak sweep-om na :00). **ODLUKA: SOFT-DELETE** (`is_deleted=true` + `text=NULL` + brisanje reakcija), NE hard DELETE reda — jer: (a) čuva Chat Hours rekonstrukciju iz poruka (T3.11 foldira preko SVIH poruka → hard delete bi srušio „83h" brojač), (b) daje red za „Deleted message…" placeholder (T3.14), (c) izbegava FK landmin `messages.reply_to → messages` (NO ACTION) kad noviji neistekli odgovor pokazuje na isteklu poruku. **Concurrency („da se ne obrišu sačuvani"):** cutoff se računa PO konverzaciji iz aktuelnog `saved` u ISTOM MVCC snapshot-u kao brisanje (single-statement CTE) → toggle tokom sweep-a vidi dosledan snapshot; `is_deleted=false` guard = idempotentno. **Anti-drift (kao T3.6):** SQL `interval '24 hours'`/`'30 days'` veran port `EPHEMERAL_DEFAULT_MS`/`EPHEMERAL_SAVED_MS` + `expiryCutoff`, zakovan novim pinning testom u `ephemeral.test.ts`. Primenjeno live (psql/pooler), cron jobid 2, `messages` tabela prazna (0 redova) → sweep bezopasan, vratio 0. Tipovi regenerisani (`saved` u Row/Insert/Update). `tsc` čist, ESLint 0, **test 141/141** (+3). ⚠️ Watch: (1) storage objekti (media fajlovi u bucket-ima) se NE brišu — SQL nulira samo `text`; pravo čišćenje bucket-a traži edge function/klijent (kandidat za kasnije); (2) soft-delete čuva metapodatke (sender/created_at) trajno — „ephemeral sadržaj", ne hard removal. *Odblokira T3.14 (toggle UI + placeholder).*
- [x] **T3.14** ✅ "Save chat" 30d toggle + "Deleted message..." placeholder. **Bez nove lib logike** — wiring `conversations.saved` (T3.13) + soft-delete render. (1) **Save chat toggle** (`components/ChatAdditionalMedia.tsx`, 3-tačke meni Chat 5.8): toggle je ranije bio samo lokalni `useState` (ništa nije čuvao) → sad prima `conversationId`, na otvaranju sheet-a učita `conversations.saved`, na promenu optimistički upiše (revert na grešku). ON=30d / OFF=24h retencija (`lib/ephemeral.ts`). (2) **"Deleted message..." placeholder**: `is_deleted` dodat u `Message` tip + mapiran u `MessageContext` (fetch + realtime INSERT). `chat-room/[id].tsx` `renderMessageContent` sad renderuje italic placeholder kad je `item.is_deleted` — **pre svih tipova** (slika/audio/file), nezavisno od `text` (cron soft-delete T3.13 postavlja `text=NULL`, klijentski delete postavlja literal); pokriva oba izvora brisanja. `tsc` čist, ESLint 0 errors, test 141/141 (logika nepromenjena). ⚠️ Mute/No Blahs/Block/Pin toggle-i u istom meniju su i dalje lokalni-only (van obima — Chat 5.8 ostatak / T4.4).
- [x] **T3.15** ✅ Tap-to-View media (view-once → "Opened" stanje — Chat 5.4). **Mala lib odluka + wiring.** `lib/tapToView.ts` (čista, §2.5): stanje se izvodi iz JEDNOG polja `messages.opened_at` (null→`tap-to-view`, set→`opened`); `getTapToViewState`/`isTapToViewOpened`/`canOpenTapToView({isSender,openedAt})` (samo primalac, samo dok nije otvoreno — pošiljalac NIKAD ne otvara svoj snap)/`tapToViewLabel` (primalac "Tap to View" / pošiljalac "Delivered" / oba "Opened")/`isTapToViewType` (`TAP_TO_VIEW_TYPES=['image']` single source za anti-drift kad dođe 'video'). Test `lib/tapToView.test.ts` (7 grupa). **DB:** migracija `20260625120000_tap_to_view_opened_at.sql` (primenjena live): kolona `messages.opened_at` (timestamptz, nullable) **+ RLS `update_conversation_messages`** — messages do sada NIJE imao NIJEDNU UPDATE politiku (samo insert/select) uz enable-ovan RLS → svaki klijentski UPDATE je bio tiho odbijan (lomilo i postojeći `handleDelete` soft-delete!); nova politika dozvoljava učesnicima konverzacije (pošiljalac/primalac) update → primalac upisuje `opened_at`, pošiljalac soft-delete. **Wiring:** `Message.opened_at` (tip); `MessageContext` mapira `opened_at` (fetch+realtime), **novi realtime UPDATE handler** (sender vidi "Opened" uživo + reflektuje cron soft-delete T3.13), `markMessageOpened(messageId,userId)` (optimistički + `opened_at IS NULL`/`sender_id != userId` guard = idempotentno, samo primalac); `components/ImageMessage.tsx` više NE prikazuje sliku inline — placeholder bubble (eye ikona + labela), primalac tapne → otvara viewer JEDNOM + markira opened → pređe u sivo "Opened" i ne može ponovo; `chat-room/[id].tsx` prosleđuje `openedAt`/`onOpen`. `tsc` čist, ESLint 0 errors, **test 155/155** (+14). ⚠️ Watch: (1) `ChatAdditionalMedia` „shared media" galerija (`chatImages`) i dalje lista URL-ove slika trajno → zaobilazi ephemeral „view-once" (van obima, kandidat za cleanup); (2) nema `video` message_type još — tap-to-view trenutno samo `image`.

**Presence + Close-By**
- [ ] **T3.16** 🧪 `lib/presenceMessages.ts` (randomizovane poruke po zonama + zaokruživanje vremena) + test
- [ ] **T3.17** Presence randomizovane poruke u UI (zamena "gone exploring")
- [ ] **T3.18** 🧪 `lib/closeBy.ts` (radius 20–30m geo obračun) + test
- [ ] **T3.19** ☁️ Close-By discovery UI + Supabase geo upit (labela "Close By")

**Premium / Blah+**
- [ ] **T3.20** ⚡ Premium gating sistem (provera Blah+ statusa) + paywall integracija (4.6/8.6/8.7) — *cross-cutting, dodiruje ceo app; profil deo zavisi od T1.9*
- [ ] **T3.21** ☁️ Who viewed profile (tracking poseta + lista 8 dana) — MyProfile 8.3 — *zavisi od T1.9*
- [ ] **T3.22** Score Boost +10% za premium korisnike
- [ ] **T3.23** Stories 24h expiry + "Lock 3 posts forever" gating
- [ ] **T3.24** Ad-free (reklamni sistem placeholder + gating)

## FAZA 4 — Polish / preostalo
- [ ] **T4.1** 🧠 Auth uskladiti sa spec-om (ODLUKA: telefon-first + Twitter/Instagram/Apple)
- [ ] **T4.2** Notifikacije: svi tipovi (D1–D5) + deep-link na ekran
- [ ] **T4.3** ☁️ Push (Firebase) kompletiranje
- [ ] **T4.4** Settings & Privacy toggle-ovi (private, who-can-message, last seen, location, mutes)
- [ ] **T4.5** Legal stranice render (Terms, Privacy, Community Guidelines, Contact)
- [ ] **T4.6** ⚡ Multi-account / Switch account (+ per-nalog badge) — *upravljanje više sesija, lako se zezne*
- [ ] **T4.7** Report account flow (kategorije + obrada — Profile 7.5)
- [ ] **T4.8** Komentari: sortiranje po lajkovima + reply threadovi + lajk komentara (Home 2.4)
- [ ] **T4.9** Share na eksterne app-ove (WhatsApp/IG/Messenger/Snapchat — Home 2.1)
- [ ] **T4.10** 🧪 Grid layout algoritam (neparni 143px / parni 123px — MyProfile 8.0) + test
- [ ] **T4.11** Boje u temu (`constants/Colors`) — cleanup hardkodovanih hex
- [ ] **T4.12** Razbiti velike fajlove (`chat-room/[id].tsx` 1252 linije, `MessageContext` 650)
- [ ] **T4.13** ☁️ **E2E sa Maestro** — smoke-test kritičnih flow-ova na emulatoru/uređaju (login → home, pošalji blah, recovery popup). *Namerno u Fazi 4:* flow-ovi su krhki dok se ekrani menjaju (profil se konsoliduje u T1.8/T1.9), pa E2E ima smisla tek na **stabilizovanim** flow-ovima. Maestro (YAML flow-ovi, lak setup, EAS Build CI integracija) izabran umesto Detox-a (teži setup) / Cypress-a (samo web build, ne pravi native). Pure logika ostaje pokrivena `lib/` Jest testovima (Faza 3); RNTL opciono za component ponašanje.

---

## 🧠 Odluke koje blokiraju određene taskove
- **T1.8 (profil struktura)** — ✅ **REŠENO: 2 ekrana** po Figma spec-u (7.x tuđi / 8.x svoj), ne jedan unify ekran. Base: `profile/index.tsx` (svoj) + `profile/profile-details/[id].tsx` (tuđi); `profile/test/[id].tsx` obrisan u T1.9. Feature-matrica (✅=ima, ❌=nema/pokvareno):

  | Feature | `index.tsx` svoj | `test/[id]` unify | `profile-details/[id]` tuđi |
  |---|---|---|---|
  | Grid postova | ✅ | ❌ zakomentarisan | ✅ |
  | Edit profil | ✅ | ✅ | — |
  | Follow/Message | — | ✅ | ✅ |
  | Block/Mute/Report | — | ✅ | ✅ |
  | Private lock (7.0) | — | — | ✅ |
  | 3-tačke→Settings (8.0) | ❌ logout modal | ✅ | — |
  | Realtime subscribe | ✅ | — | — |
  | Stanje koda | čist, radi | prototip, grid mrtav | čist, radi |

  **Zašto 2 a ne 1:** spec ih eksplicitno deli; razlike su suštinske (svoj: Settings/Eye/Recovery vs tuđi: Follow/Block/private-lock) → unify = komponenta prošarana `isOwnProfile ?` (baš `test/[id]`, haotičan i pokvaren). Najmanji rizik: oba base fajla rade, briše se samo 1 fajl + 2 rewire.
- **T3.6 (streak reset job)** — ✅ **REŠENO: pg_cron + tanak SQL sweep** (ne edge function, ne samo on-read). Razmatrano vs (a) Edge Function koji uvozi lib (deploy iz Win/bez-Docker rizičan + scheduling i dalje traži pg_cron), (b) samo on-read lazy reset (bez prave background infre → ☁️ deo T3.6 i T3.9 notif odloženi). Izabran cron jer task eksplicitno traži server-side job i odblokira T3.9. **Anti-drift tenzija** (hard rule „logika u lib/, ne duplirati u SQL", v. T3.3): ublažena tako što SQL portuje SAMO JEDNU granicu reseta (`dayKey` gap ≥2 dana), zakovanu **pinning testom** u `lib/streak.test.ts`; sva ostala streak math ostaje u `lib/`. On-read lazy reset dodat kao backup za trenutnu tačnost UI-ja.
- **T3.8 (recovery plaćanje)** — ✅ **REŠENO: stubbed purchase boundary** (ne pravi RevenueCat sada, ne odlaganje plaćanja). RevenueCat API ključevi + €1.99 store proizvod (App Store / Play) ne postoje u dev okruženju i ne mogu se provizionisati ovde. Razmatrano vs (a) pun RevenueCat odmah — traži da korisnik konfiguriše proizvode/ključeve pre koda, (b) samo UI + countdown, plaćanje u zaseban task — cepa nerazdvojivu celinu. Izabran stub jer drži task samo-sadržanim i testabilnim: čista logika (`applyRecovery`/`formatRecoveryCountdown` + test) i ceo tok (popup → naplata → vraćanje streak-a → upis) rade na uređaju; jedini stub je IO granica `services/recoveryPurchase.ts` (`RECOVERY_PURCHASE_STUBBED=true`) sa istim potpisom kao pravi tok → swap je flip prekidača + otkomentarisanje `Purchases.purchasePackage`. Pravilo „logika u lib/" poštovano (plaćanje je side-effect → `services/`, ne `lib/`).
- **T4.1 (auth)** — čeka odluku telefon-vs-email. *Ne blokira Fazu 0–3.*
- **Vidljivost followers liste** za privatne naloge (Figma beleška) — utiče na Profile 7.3/7.4.

## 📝 Napomena o proceni
~54 task je **grubа** procena; neki Faza-3 taskovi (npr. ephemeral, premium gating) mogu da se razbiju na više pod-taskova kad uđemo u njih. Faza 3 je 50%+ ukupnog posla.
